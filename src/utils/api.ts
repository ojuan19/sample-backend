export const fetchPolls = async () => {
  // TODO: Fetch polls from API
  return [];
};

export const createPoll = async (question: string, options: string[]) => {
  // TODO: Create a new poll
};

export const votePoll = async (pollId: string, optionId: string) => {
  // TODO: Submit vote for a poll
};